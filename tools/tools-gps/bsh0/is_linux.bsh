#!/bin/bash
#############################################################################
# Invocation:
#               ( is_linux ) && echo LINUX
#############################################################################
if   [[ ${OSTYPE:0:4} == 'irix'  ]]; then exit 1
elif [[ ${OSTYPE:0:5} == 'linux' ]]; then exit 0
                                     else exit 1
fi
