#!/bin/bash

#############################################################################
# Invocation:
#               ( is_irix ) && echo IRIX
#############################################################################
if   [[ ${OSTYPE:0:4} == 'irix'  ]]; then exit 0;
elif [[ ${OSTYPE:0:5} == 'linux' ]]; then exit 1;
                                     else exit 1;
fi
